/* exported setValue */

// make an exisiting object the first index of an array
// make index of key into the property of the object
// assign index of value as the value of the object

function setValue(object, key, value) {
  object[key] = value;
}
