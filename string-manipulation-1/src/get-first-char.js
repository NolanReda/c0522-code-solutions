/* exported getFirstChar */

// create a funciton to return the first character of any string.
// check the value at the 0 index of the string and give back that values

function getFirstChar(string) {
  return string.charAt(0);
}
