/* exported getLastChar */

// create a function to return last character of any string
// check the value at the end of a string and return it from the function

function getLastChar(string) {
  return string.charAt(string.length - 1);
}
