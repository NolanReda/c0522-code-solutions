/* exported getWords */

// take each value separated by spaces from a string value
// convert them over to a new array
// return new array

function getWords(string) {
  var words = [];
  return words.push(string.slice(' '));
}
